import{contentPackSchema,type ContentPackDocument}from"@/src/domain/content-pack";export interface ValidationResult{success:boolean;data?:ContentPackDocument;errors:Array<{path:string;message:string}>;warnings:string[]}const forbidden=new Set(["__proto__","prototype","constructor"]);function inspect(value:unknown,depth=0):void{if(depth>80)throw new Error("Import nesting is too deep");if(!value||typeof value!=="object")return;for(const[key,child]of Object.entries(value)){if(forbidden.has(key))throw new Error(`Forbidden object key: ${key}`);inspect(child,depth+1)}}export function validateContentPackJson(json:string,maxBytes=25*1024*1024):ValidationResult{if(new TextEncoder().encode(json).byteLength>maxBytes)return{success:false,errors:[{path:"",message:"File exceeds the import size limit"}],warnings:[]};try{const parsed:unknown=JSON.parse(json);inspect(parsed);const result=contentPackSchema.safeParse(parsed);if(!result.success)return{success:false,errors:result.error.issues.map(i=>({path:i.path.join("."),message:i.message})),warnings:[]};const ids=[...result.data.sources.map(s=>s.id),...result.data.entries.map(e=>e.id)];const duplicates=[...new Set(ids.filter((id,i)=>ids.indexOf(id)!==i))];return{success:true,data:result.data,errors:[],warnings:duplicates.length?[`Duplicate ids: ${duplicates.join(", ")}`]:[]}}catch(error){return{success:false,errors:[{path:"",message:error instanceof Error?error.message:"Invalid JSON"}],warnings:[]}}}