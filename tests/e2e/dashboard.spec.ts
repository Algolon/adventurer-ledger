import{expect,test}from"@playwright/test";test("opens and advances Brammel builder",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:"Brammel “Boss” Voss"})).toBeVisible();await page.getByRole("button",{name:/Continue character/}).click();await expect(page.getByRole("dialog",{name:"Brammel “Boss” Voss"})).toBeVisible();await page.getByRole("button",{name:/Continue/}).click();await expect(page.getByText("Step 6 of 6")).toBeVisible()});test("navigates to sources",async({page})=>{await page.goto("/");await page.getByRole("button",{name:"Sources",exact:true}).click();await expect(page.getByRole("heading",{name:"Source management"})).toBeVisible()});