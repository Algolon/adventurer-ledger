import type{Metadata,Viewport}from"next";import"./globals.css";import"./m1.css";import{PwaProvider}from"@/src/ui/pwa-status";import{withBasePath}from"@/src/config/base-path";export const metadata:Metadata={title:"Adventurer Ledger",description:"Private local-first D&D character builder",manifest:withBasePath("/manifest.webmanifest"),icons:{apple:withBasePath("/icons/icon-192.png")},appleWebApp:{capable:true,statusBarStyle:"black-translucent",title:"Ledger"}};export const viewport:Viewport={themeColor:"#111a22"};export default function Layout({children}:{children:React.ReactNode}){return<html lang="en"><body data-app-build={process.env.NEXT_PUBLIC_BUILD_LABEL??"production"}><PwaProvider>{children}</PwaProvider></body></html>}
