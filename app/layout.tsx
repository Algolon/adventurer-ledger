import type{Metadata}from"next";import"./globals.css";export const metadata:Metadata={title:"Adventurer Ledger",description:"Private local-first D&D character builder",manifest:"/manifest.webmanifest"};export default function Layout({children}:{children:React.ReactNode}){return<html lang="en"><body>{children}</body></html>}